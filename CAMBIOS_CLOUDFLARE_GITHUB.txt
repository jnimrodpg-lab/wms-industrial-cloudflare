WMS Cloudflare/GitHub - corrección importación completa

Versión: cloudflare-v5-import-50000-search-api

Cambios principales:
1. La lectura de Google Sheets en Cloudflare usa CSV completo antes de fallback JSON GViz.
2. El límite de importación queda en 50,000 filas.
3. Se agregó API real para búsqueda/paginación en Cloudflare:
   - GET /api/branches/:id/products
   - GET /api/branches/:id/products-summary
4. El backend ya no queda limitado al lote inicial de 500 registros.
5. Se conserva la corrección de estabilidad del layout para reducir el temblor al mover racks/zonas.
6. Se actualizó el cache-buster del script principal para forzar que Cloudflare cargue la versión nueva.

Importante después de subir:
- En Cloudflare Pages, vuelve a desplegar desde GitHub.
- En la app, entra como administrador y vuelve a importar el Sheet usando REEMPLAZAR.
- Si antes se habían importado solo 500 productos, esa importación antigua debe reemplazarse para que el buscador trabaje con toda la hoja.

Cuenta inicial por defecto si usas las variables del wrangler.toml:
Usuario: admin
Contraseña: admin123
